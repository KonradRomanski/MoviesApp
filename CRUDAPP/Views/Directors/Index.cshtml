@model List<CRUDAPP.Models.Domain.Director>

<h1>Directors list</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Surname</th>
            <th>Date of birth</th>
            <th>Date of death</th>
            <th>Rating</th>
            <th> </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var director in Model)
        {
            <tr>
                <td>@director.Id</td>
                <td>@director.Name</td>
                <td>@director.Surname</td>
                <td>@director.DateOfBirth.ToString("ddd dd MMMM yyyy")</td>
                <td>@(director.DateOfDeath?.ToString("ddd dd MMMM yyyy") ?? "-")</td>
                <td>@director.Rating</td>
                <td><a type="button" class="btn btn-light" href="Movies/View/@director.Id" Views>View</a></td>
            </tr>
        }
    </tbody>
</table>

